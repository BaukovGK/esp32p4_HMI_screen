# ESP-IDF build artefacts and managed dependencies
build/
managed_components/
sdkconfig.old

# Не игнорируем sdkconfig — он коммитится как baseline для проекта
# Если хотите игнорировать — переименуйте default в sdkconfig.defaults

# IDE / editor
.vscode/
.idea/
*.swp
*~

# macOS
.DS_Store
**/.DS_Store

# Windows
Thumbs.db

# Локальные конфиги/секреты
*.local.*
secrets.h
secrets.yaml.local

# Claude Code local cache
.claude/

# clangd / LSP
.clangd
.cache/
